<h1>Listing subscribers</h1>

<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Account number</th>
      <th>Property address</th>
      <th>Property city</th>
      <th>Property state</th>
      <th>Property zip</th>
      <th>Subscription size</th>
      <th>Email</th>
      <th>Phone</th>
      <th>Secondary contact</th>
      <th>Secondary phone</th>
      <th>Secondary email</th>
      <th>Payment details</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @subscribers.each do |subscriber| %>
      <tr>
        <td><%= subscriber.first_name %></td>
        <td><%= subscriber.last_name %></td>
        <td><%= subscriber.account_number %></td>
        <td><%= subscriber.property_address %></td>
        <td><%= subscriber.property_city %></td>
        <td><%= subscriber.property_state %></td>
        <td><%= subscriber.property_zip %></td>
        <td><%= subscriber.subscription_size %></td>
        <td><%= subscriber.email %></td>
        <td><%= subscriber.phone %></td>
        <td><%= subscriber.secondary_contact %></td>
        <td><%= subscriber.secondary_phone %></td>
        <td><%= subscriber.secondary_email %></td>
        <td><%= subscriber.payment_details %></td>
        <td><%= link_to 'Show', subscriber %></td>
        <td><%= link_to 'Edit', edit_subscriber_path(subscriber) %></td>
        <td><%= link_to 'Destroy', subscriber, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Subscriber', new_subscriber_path %>
